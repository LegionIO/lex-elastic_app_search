RSpec.describe Legion::Extensions::ElasticAppSearch do
  it 'has a version number' do
    expect(Legion::Extensions::ElasticAppSearch::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
